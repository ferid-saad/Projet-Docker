apiVersion: v1
kind: Service
metadata:
  name: contacts-mysql-service
spec:
  selector:
    app: contacts-mysql
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
    name: mysql