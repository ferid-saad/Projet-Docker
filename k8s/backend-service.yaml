apiVersion: v1
kind: Service
metadata:
  name: contacts-backend-service
spec:
  selector:
    app: contacts-backend
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    name: http